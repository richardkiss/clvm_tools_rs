(mod ()
     (include *standard-cl-21*)
     (include sha256tree.clib)
     (defun increment-list (L)
       (if L
         (c (+ 1 (f L)) (increment-list (r L)))
         ()
         )
       )
     (defun print (prints result) result)
     (defun use_result (sha)
       (print (list "sha256_returned" sha) sha)
       )
     (use_result (sha256tree (print "calling_sha256tree" (increment-list (list 1 2 3)))))
     )
